# Trabalho 2

Documentação resumida do pipeline do trabalho 2.

- Extrai todas as tabelas do PostgreSQL para o bucket `landing-zone` em CSV.
- Converte os CSVs do bucket `landing-zone` para Delta Lake no bucket `bronze`.
- Reproduz operações de `insert`, `update` e `delete` em uma tabela Delta do bucket `bronze`.

Consulte o `README.md` para o passo a passo completo.
